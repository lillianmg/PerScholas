{
  "name": "spaceship",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "predeploy": "npm run build",
    "deploy": "gh-pages -b main -d build"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}
